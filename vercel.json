{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "rm -rf node_modules && npm install && npx vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "env": {
    "VITE_API_URL": "https://backend-production-1fae9.up.railway.app/api"
  }
}
